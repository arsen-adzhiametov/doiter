<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:windowBackground">@drawable/bg_repeatable</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!--Gabriola font theme-->
    <style name="GabriolaGrayFontStyle">
        <item name="android:textSize">@dimen/gabriola_text_size</item>
        <item name="android:textColor">@color/gray_text_color</item>
    </style>

    <style name="GabriolaOrangeFontStyle">
        <item name="android:textSize">@dimen/gabriola_text_size</item>
        <item name="android:textColor">@color/orange_seek_thumb_color</item>
    </style>

</resources>
